mutation initializeRelease {
  initializeRelease {
    release {
      id
    }
  }
}
mutation updateRelease($data: UpdateReleaseInput!) {
  updateRelease(data: $data) {
    release {
      id
      name
      tracks {
        id
      }
    }
  }
}
